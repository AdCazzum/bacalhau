.direnv/
result
node_modules/
dist/
out/
cache/
broadcast/
target/
.env
.env.*
!.env.example
# Local fork deployments are regenerated on every `nix run .#dev`; the public
# Base Sepolia record is tracked (it is what the subgraph indexes).
deployments/local.json
app/public/local.json
contracts/broadcast/
contracts/cache/
